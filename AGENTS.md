始终使用简体中文回复和思考。

## Agent skills

### Issue tracker

GitHub Issues（仓库：zeshuai007/web-game1），使用 `gh` CLI。详见 `docs/agents/issue-tracker.md`。

### Triage labels

标准五态：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单一上下文：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
