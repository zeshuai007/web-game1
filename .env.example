# 仙逆放置修仙 — 环境变量

## 必需
JWT_SECRET=your-strong-random-secret-here
DB_CONNECTION_STRING=postgresql://user:pass@host:port/db

## 可选（Pusher 实时聊天）
PUSHER_ENABLED=0
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap1
