// game-engine.ts (barrel) removed — data moved to DB, logic moved to config.ts and realm-config.ts.
// Update imports:
//   - realm-config.ts for getMaxLayer / getNextRealm / isMaxLayer / Realm / realmEnum
//   - config.ts for getRealmFromDB / getQualityConfigFromDB / rollAdventureEventWithConfig etc.
//   - cultivation-balance.ts for resolveMajorBreakthrough
